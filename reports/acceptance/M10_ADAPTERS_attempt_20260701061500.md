# Acceptance Report: M10_ADAPTERS

- Module: `M10_ADAPTERS`
- Attempt: `attempt_20260701061500`
- Agent: `module-impl-agent-M10`
- Date: `2026-07-01T06:15:00Z`

## Scope completed

Implemented adapter bundle generation for Codex, Copilot, and OpenCode as a
pure file generator (no custom integration runtime):

- `render_codex_bundle(project_root) -> dict[str, str]`
- `render_copilot_bundle(project_root) -> dict[str, str]`
- `render_opencode_bundle(project_root) -> dict[str, str]`
- `export_agent_bundle(target, out=None, *, store=None, project_root=None) -> list[Path]`

Wired the previously-stubbed `dmc export-agent-bundle` CLI command (owned by
M10 per the bootstrap CLI comment) to the new adapter module, and closed the
Path-serialization gap in M09's lazy `dmc_export_agent_bundle` MCP wrapper now
that `dmc.adapters` genuinely exists.

## Files changed

Created:
- `src/dmc/adapters.py`
- `tests/test_adapters.py`

Modified:
- `src/dmc/cli.py` — implemented the `export-agent-bundle` command (previously
  a `_not_implemented` placeholder owned by M10).
- `src/dmc/mcp_server.py` — `dmc_export_agent_bundle` now stringifies the
  `list[Path]` result from `export_agent_bundle` into a JSON-compatible
  envelope instead of passing raw `Path` objects through; removed the
  `# pragma: no cover - needs M10` comment on the now-reachable delegation
  branch.
- `tests/test_cli.py` — added `export-agent-bundle` success/failure tests.

## Public APIs implemented

```python
AdapterTarget = Literal["codex", "copilot", "opencode"]
VALID_TARGETS: tuple[str, ...]

def render_codex_bundle(project_root: str | Path) -> dict[str, str]: ...
def render_copilot_bundle(project_root: str | Path) -> dict[str, str]: ...
def render_opencode_bundle(project_root: str | Path) -> dict[str, str]: ...
def export_agent_bundle(
    target: AdapterTarget,
    out: str | Path | None = None,
    *,
    store: object | None = None,
    project_root: str | Path | None = None,
) -> list[Path]: ...
```

Bundle contents:

- Codex: `AGENTS.md`, `.codex/config.toml.template`, `.dmc/adapters/codex/README.md`
- Copilot: `.github/copilot-instructions.md`, `.github/skills/dmc-start-task/SKILL.md`, `.dmc/adapters/copilot/README.md`
- OpenCode: `AGENTS.md`, `opencode.jsonc.template`, `.dmc/adapters/opencode/agents/dmc-agent.md`, `.dmc/adapters/opencode/skills/dmc-start-task/SKILL.md`, `.dmc/adapters/opencode/README.md`

Every generated protocol file instructs the agent to: call
`dmc_plan_task`/`dmc_get_briefing` at task start, use Serena/GitHub/Sourcegraph/
Basic Memory MCP servers if configured, call `dmc_precheck` before risky
actions, call `dmc_record_event` after meaningful actions, call
`dmc_commit_state` at checkpoints, call `dmc_distill_session` at session end,
and never directly mutate accepted skills (propose via
`dmc_propose_skill_update` instead).

## Tests added or changed

- `tests/test_adapters.py` (16 tests): expected file sets per target,
  determinism, no-dynamic-project-facts invariant, frontmatter shape for
  SKILL.md/agent files, disk writes (incl. parent-dir creation), content
  parity between renderer output and written files, invalid-target
  `ValueError`, root resolution from `store.root`, explicit `project_root`,
  and default-to-`cwd`.
- `tests/test_cli.py` (+2 tests): `export-agent-bundle` writes files and
  echoes paths; invalid target exits non-zero.
- `tests/test_mcp_server.py`: no changes needed — the existing
  `test_export_agent_bundle_lazy_fallback` test now exercises the "M10
  present" delegation branch instead of the "M10 absent" branch, and still
  passes.

## Commands run

| Command | Result | Notes |
|---|---|---|
| `uv run pytest tests/test_adapters.py -q` | pass | 16 passed |
| `uv run pytest -q` | pass | 257 passed (full suite, up from 239 at M09) |
| `uv run ruff check .` | pass | All checks passed |
| `uv run dmc --help` | pass | exit 0; `export-agent-bundle` now documented |
| `uv sync` | pass | no dependency changes |

## Acceptance checklist

```text
[x] All module commands passed.
[x] Tests include positive and negative cases.
[x] No forbidden V0 system was implemented.
[x] No dynamic facts were inserted into stable instruction files.
[x] Handoff report written.
[x] agent_state.json updated.
```

## Known limitations

- `export_agent_bundle` does not merge bundles: if both `codex` and
  `opencode` are exported to the same `out` directory, the second call's
  `AGENTS.md` overwrites the first's (both files are protocol-identical in
  content today, so this is currently a no-op collision, but a future change
  that diverges the two would need an explicit merge policy).
- No `.codex/`, `.github/skills/`, or `.dmc/adapters/opencode/` directories
  are pre-existing in this repo's own tree; bundle generation is exercised
  only against temp directories in tests, per module scope ("output is
  files/instructions/config templates, not a custom integration runtime" —
  this repo does not consume its own generated bundles).

## Dependency changes

None. `src/dmc/adapters.py` uses only `pathlib` and `typing` from the
standard library. No entries added to `pyproject.toml` or `uv.lock`.

## Evidence links

- [reports/handoffs/M10_ADAPTERS_attempt_20260701061500.md](../handoffs/M10_ADAPTERS_attempt_20260701061500.md)
